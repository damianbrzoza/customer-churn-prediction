"""Setup file."""
from setuptools import find_packages, setup

setup(
    name="project_name",
    version="0.0.0",
    author="Damian Brzoza",
    install_requires=[],
    packages=find_packages(),
)